// Configuration file for environment and strategy settings

// Environment settings
export const environment = {
    production: false,
    apiUrl: 'http://localhost:3000/api',
};

// Strategy settings
export const strategy = {
    buyThreshold: 0.01,
    sellThreshold: 0.02,
};
